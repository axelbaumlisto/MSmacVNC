round 1: strategic — 3 blind critics (macOS platform / failure-modes / security), all CONDITIONAL GO. 13 P0 findings; plan rewritten: updateContentFilter instead of stream restart, tag injected events or remote control breaks, secure-input lift-on-transition (password-leak path), non-opaque curtain (occlusion freezes the remote stream), collectionBehavior for Spaces/full-screen, hang != crash so watchdog + no-block callback, capped throttle, 8-byte DES comparison, refuse to arm without a password, edge-triggered raise (level-triggered made the escape hatch a no-op), half-open TCP caveat.
round 2: strategic — 2 critics (implementation / adversarial safety). Both NO-GO for the tap task, GO for policy+window. New P0s: TCC-degraded tap returns non-NULL with keyboard bits stripped; the "two input paths" were mutually exclusive and the fix broke remote typing; watchdog measured silence (lifts on idle) and could not tear down a window from a background queue; stream death AFTER raise unhandled; secure-input check-then-act race; reconnect re-blinds after a local lift; window-ID exclusion needs the window on screen first. Plan restructured to 5 tasks: policy, window (exclude by APPLICATION), a testable controller, then the tap; fail-open re-grounded on "input fails open by itself, the screen only via process exit".
